const math = {
    E: 2.718281828459045,

    add: function add(a, b) {
        return a + b
    },
     sub(a, b) {
        return a - b
    },
    square: function sqr(a) {
        return a*a
    },
    cube(a){
        return a**3
    }
}

// These all are methods. When a function is put inside an object that function is called Methods
// Every method is a function but every functions are not a method

